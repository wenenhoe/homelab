name: cleanup_app_a
services:
  cleanup_app_a:
    image: alpine:3.20
    volumes:
      - cleanup_app_a_data:/data
    command: [ "sh", "-c", "sleep infinity" ]
volumes:
  cleanup_app_a_data:
    external: true
