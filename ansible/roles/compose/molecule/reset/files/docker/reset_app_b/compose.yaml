name: reset_app_b
services:
  reset_app_b:
    image: alpine:3.20
    volumes:
      - reset_app_b_cache:/cache
    command: [ "sh", "-c", "sleep infinity" ]
volumes:
  reset_app_b_cache:
    external: true
