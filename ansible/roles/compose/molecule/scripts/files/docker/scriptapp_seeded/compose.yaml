name: scriptapp_seeded
services:
  scriptapp_seeded:
    image: alpine:3.20
    volumes:
      - scriptapp_seeded_scripts:/scripts
    command: [ "sh", "-c", "sleep infinity" ]
volumes:
  scriptapp_seeded_scripts:
    external: true
