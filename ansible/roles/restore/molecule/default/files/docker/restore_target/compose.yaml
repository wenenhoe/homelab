name: restore_target
services:
  restore_target:
    image: alpine:3.20
    container_name: restore_target
    volumes:
      - data:/data
    command: [ "sh", "-c", "sleep infinity" ]
volumes:
  data:
    external: true
    name: restore_target_data
