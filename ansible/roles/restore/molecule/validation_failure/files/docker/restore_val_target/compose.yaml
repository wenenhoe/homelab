name: restore_val_target
services:
  restore_val_target:
    image: alpine:3.20
    container_name: restore_val_target
    volumes:
      - data:/data
    command: [ "sh", "-c", "sleep infinity" ]
volumes:
  data:
    external: true
    name: restore_val_target_data
