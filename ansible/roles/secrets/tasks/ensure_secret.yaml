---
# Generates (first run) or reuses (every run after) every secret in
# secrets_items, merging each value into secrets_generated. Included by
# main.yaml twice: once with secrets_items: [main-domain], once with
# every other registry key at once — not once per key, so this file's
# own task list runs twice per playbook run, not once per secret.
#
# manual, no vault_scope (the controller-side file cache — as of Track
# A stage 6, exactly the three permanent bootstrap exceptions:
# main-domain and the two openbao-controller AppRole halves; every
# other manual entry now has a vault_scope) is handled directly below,
# each task looping over secrets_items itself. vault_scope present
# (every hex/uuid4 entry, and every other manual entry) is delegated to
# process_vault_secrets.yaml, included once per call to this file (not
# once per secret) — it does its own internal per-task looping the same
# way, correlating results across its several read/generate/write
# stages by secret name rather than by list position, since each
# stage's loop covers a different, independently-filtered subset. See
# secrets_registry.yaml's own header comment and
# docs/decisions/0021-vault-path-convention-hosts-all-for-global-secrets.md
# for the vault_scope split itself.
#
# Every task here needs both delegate_to: localhost and tags: [always],
# even plain set_fact/fail tasks: a task with no delegate_to still
# triggers remote_addr resolution for the current play host before
# dispatching, and this role runs before secrets_generated exists (the
# value every real host's ansible_host depends on — see
# docs/secrets.md). Tags on include_role/include_tasks don't propagate to
# untagged child tasks under a --tags filter, so every level of inclusion
# needs its own explicit tag.
#
# become: false is explicit everywhere as a defensive default in case a
# future caller includes this role from a play with become: true at the
# play level (inherited regardless of delegate_to otherwise).
#
# no_log: true wherever a task result could put the plaintext secret into
# -v/-vvv output.

# =========================================================================
# --- manual format, file-cache-backed: main-domain and the controller --
# --- AppRole pair only, see this file's own header comment -------------
# =========================================================================
# Nothing here can generate these values — each is externally-issued or
# plain config supplied ahead of time. The cache file must already
# exist; if not, fail loudly with what's missing and how to fix it. An
# existing file with empty content is valid (`allow_blank` in
# secrets_registry.yaml) — existence, not content, is what's checked.

- name: "Check whether manual secret files exist"
  vars:
    secrets_cache_path: "{{ project_root }}/ansible/files/secrets/{{ item }}"
  ansible.builtin.stat:
    path: "{{ secrets_cache_path }}"
  loop: "{{ secrets_items }}"
  loop_control:
    label: "{{ item }}"
  when:
    - secrets_registry[item].format == 'manual'
    - secrets_registry[item].vault_scope is not defined
  register: secrets_manual_stat
  delegate_to: localhost
  become: false
  tags: [always]

- name: "Fail loudly if a manual secret was never supplied"
  vars:
    secrets_cache_path: "{{ project_root }}/ansible/files/secrets/{{ item.item }}"
  ansible.builtin.fail:
    msg: |
      Missing required secret: {{ item.item }}
      {{ secrets_registry[item.item].description | default('(no description in secrets_registry.yaml)') }}
      Create it with:
        printf '%s' '<value>' > {{ secrets_cache_path }} && chmod 600 {{ secrets_cache_path }}
      Or run once to fill in every missing manual secret interactively:
        cd tools && python3 -m openbao_utils.bootstrap
  loop: "{{ secrets_manual_stat.results | default([]) }}"
  loop_control:
    label: "{{ item.item }}"
  when:
    - item.stat is defined
    - not item.stat.exists
  delegate_to: localhost
  become: false
  tags: [always]

- name: "Read manual secret values"
  vars:
    secrets_cache_path: "{{ project_root }}/ansible/files/secrets/{{ item }}"
  ansible.builtin.slurp:
    path: "{{ secrets_cache_path }}"
  loop: "{{ secrets_items }}"
  loop_control:
    label: "{{ item }}"
  register: secrets_manual_read
  delegate_to: localhost
  become: false
  no_log: true
  tags: [always]
  when:
    - secrets_registry[item].format == 'manual'
    - secrets_registry[item].vault_scope is not defined

- name: "Store manual secrets"
  ansible.builtin.set_fact:
    secrets_generated: >-
      {{
        secrets_generated | combine({
          item.item: (item.content | b64decode | trim)
        })
      }}
  loop: "{{ secrets_manual_read.results | default([]) }}"
  loop_control:
    label: "{{ item.item }}"
  delegate_to: localhost
  become: false
  no_log: true
  tags: [always]
  when: item.content is defined

# =========================================================================
# --- Vault-backed (any format) ------------------------------------------
# =========================================================================

- name: "Process every Vault-backed secret in this call"
  ansible.builtin.include_tasks: process_vault_secrets.yaml
  tags: [always]
