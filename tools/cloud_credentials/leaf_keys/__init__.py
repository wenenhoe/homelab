"""Routine leaf-key create/rotate logic, one module per provider.

Used by create_leaf_keys.py, authenticating with the cached rotation
key (see ../rotation_keys/) - never the raw master credential.
"""
